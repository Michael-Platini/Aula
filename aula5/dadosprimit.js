// String, number, undefined, null, boolean

const nome = 'Michael'//string
const nome1 = "Michael"//string
const nome2 = `Michael`//string
const num1 = 10;//number
const num2 = 10.52;//number
let nomeAluno;// undefined = não aponta pra lugar nunhum na memória
let sobrenomeAluno = null; // Nulo -> não aponta pra local nenhum na memória
const aprovado = false; // Boolean = true, false (lógico)

console.log(typeof sobrenomeAluno, sobrenomeAluno);

